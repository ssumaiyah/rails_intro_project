<!DOCTYPE html>
<html>
<head>
  <title>Main Page</title>
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
</head>
<body>

  <div class="container mx-auto px-4 py-8">
    <h1 class="text-3xl font-bold mb-4">Welcome to the Main Page</h1>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      <% @featured_books.each do |book| %>
        <div class="border border-gray-300 rounded-lg p-4">
          <% if book.cover_image.attached? %>
            <%= link_to image_tag(url_for(book.cover_image), class: "mb-2 rounded-lg"), book_path(book) %>
          <% else %>
            <%= link_to book.title, book_path(book), class: "text-xl font-semibold mb-2" %>
          <% end %>
          <p class="text-lg text-gray-700 mb-2">by <%= link_to book.author.name, author_path(book.author) %></p>
         
        </div>
      <% end %>
    </div>


    
  </div>

</body>
</html>
